# Cómo ejecutar generator.sh

Para ejecutar generator.sh se llama, ya sea desde consola o desde un programa en go, el archivo generator.sh ya sea con el intérprete ``bash`` o cambiando antes los permisos con ``chmod +x generador.sh``:

```bash
# Con permisos
./generator.sh

# Sin permisos
bash generator.sh
```

